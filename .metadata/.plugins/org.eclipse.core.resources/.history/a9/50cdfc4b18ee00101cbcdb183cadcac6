server:  
  port: ${PORT:2222}

spring:  
  application:
    name: account-service
  datasource:
	url: jdbc:maria:mem:accountdb
	driver-class-name: org.h2.Driver
	username: sa
	password: 

logging:
  level:
    org.springframework.security: TRACE
      
security:
  user:
    name: root
    password: password
  oauth2:
    resource:
#      loadBalanced: true
      userInfoUri: http://localhost:9998/user
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      instance:
        prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
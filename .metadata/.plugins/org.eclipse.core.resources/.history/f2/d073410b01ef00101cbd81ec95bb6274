server:
  port: 5559
spring:
  application:
    name: gateway-server
  
  # FIXED: Correct Spring Cloud Gateway 2025.0.0 properties
  cloud:
    gateway:
      server:
      routes:
      - id: account-service
        uri: lb://account-service
				spring.cloud.gateway.predicate
      predicates:
        path: /account/**
          enabled: 
          

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000/user/  # Adjust to your auth server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always
